"use strict";
exports.__esModule = true;
var vector_1 = require("./vector");
var box_1 = require("./box");
var ClipFilter = /** @class */ (function () {
    function ClipFilter(matrix, eye, scene) {
        this.matrix = matrix;
        this.eye = eye;
        this.scene = scene;
    }
    ClipFilter.prototype.filter = function (v) {
        var w = this.matrix.mulPositionW(v);
        if (!this.scene.visible(this.eye, v)) {
            return [w, false];
        }
        if (!exports.ClipBox.contains(w)) {
            return [w, false];
        }
        return [w, true];
    };
    return ClipFilter;
}());
exports.ClipFilter = ClipFilter;
exports.ClipBox = new box_1.Box(new vector_1["default"](-1, -1, -1), new vector_1["default"](1, 1, 1));
